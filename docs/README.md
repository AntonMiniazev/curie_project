# Curie Documentation

Curie documentation uses PlantUML with C4-PlantUML conventions.

Source diagrams live in:

```text
docs/diagrams
```

Rendered SVG files are produced by GitHub Actions into:

```text
docs/images
```

The rendered files are generated artifacts. Edit the `.puml` files, then let the workflow refresh SVG output after push.
